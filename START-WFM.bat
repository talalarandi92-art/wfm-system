@echo off
rem  Legacy entry point kept for muscle memory. The real, maintained launcher is
rem  START-WFM.cmd (single-origin: the backend serves the UI on http://localhost:3000).
call "%~dp0START-WFM.cmd" %*
